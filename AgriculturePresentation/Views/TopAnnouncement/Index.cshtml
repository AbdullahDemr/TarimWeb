@using EntityLayer.Concrete
@model List<TopAnnouncement>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}



<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Duyuru Listesi</h4>
            <div class="table-responsive pt-3">
                <table class="table table-bordered" style="border: 2px solid black">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Başlık</th>
                            <th>Görsel</th>
                            <th>Kısa Acıklama</th>
                            <th>Acıklama</th>
                            <th>Tarih</th>
                            <th>Sil</th>
                            <th>Düzenle</th>
                            <th>Durum</th>

                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.TopAnnouncementId</td>
                                <td>@item.Title</td>
                                <td> <img src="@item.Imageurl" class="img-fluid" alt="Duyuru Görseli"></td>
                                <td>@item.Description</td>
                                <td>@item.MinDescription</td>
                                <td>@item.Date</td>
                                <td><a href="/TopAnnouncement/DeleteTopAnnouncement/@item.TopAnnouncementId" class="btn btn-outline-danger">Sil</a></td>
                                <td><a href="/TopAnnouncement/EditTopAnnouncement/@item.TopAnnouncementId" class="btn btn-outline-success">Güncelle</a></td>
                                <td>
                                    @if (item.Status)
                                    {
                                        <a href="/TopAnnouncement/ChangeStatusToFalse/@item.TopAnnouncementId" class="btn btn-outline-secondary">Pasif Yap</a>
                                    }
                                    else
                                    {
                                        <a href="/TopAnnouncement/ChangeStatusToTrue/@item.TopAnnouncementId" class="btn btn-outline-warning">Aktif Yap</a>
                                    }
                                </td>


                            </tr>
                        }
                    </tbody>
                </table>
                <a href="/TopAnnouncement/AddTopAnnouncement/" class="btn btn-outline-info">Yeni ust duyuru girişi</a>
            </div>
        </div>
    </div>
</div> 